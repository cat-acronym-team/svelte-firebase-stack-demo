{
  "scripts": {
    "prettier": "prettier -c .",
    "prettier:fix": "prettier -w .",
    "emulators": "npm --prefix functions run build && npm --prefix client run build && firebase emulators:start"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "prettier-plugin-svelte": "^2.7.0"
  }
}
